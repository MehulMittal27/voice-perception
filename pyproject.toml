[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "voice-perception"
version = "0.1.0"
description = "Real-time paralinguistic voice perception service"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
